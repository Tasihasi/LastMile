name: CI

on:
  push:
    branches: [main, production]
  pull_request:
  workflow_dispatch:

jobs:
  backend:
    name: Backend
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: backend/requirements.txt

      - name: Install dependencies
        run: pip install -r requirements.txt -r requirements-dev.txt

      - name: Lint with ruff
        run: ruff check .

      - name: Format check with ruff
        run: ruff format --check .

      - name: Validate docstrings match signatures (pydoclint)
        # Configuration lives in backend/pyproject.toml under [tool.pydoclint].
        run: pydoclint planner/

      - name: Run Django system checks
        run: python manage.py check --fail-level WARNING
        env:
          ORS_API_KEY: test-key

      - name: Run migrations
        run: python manage.py migrate
        env:
          ORS_API_KEY: test-key

      - name: Run tests with coverage
        run: |
          coverage run --source=planner manage.py test --verbosity 2
          coverage report
          coverage xml
        env:
          ORS_API_KEY: test-key

      - name: Upload coverage report
        uses: actions/upload-artifact@v4
        if: ${{ !cancelled() }}
        with:
          name: backend-coverage
          path: backend/coverage.xml
          retention-days: 7

      - name: Security scan (pip-audit)
        # Non-blocking for now -- surfaces vulns without breaking the build.
        # Flip to `continue-on-error: false` once the baseline is clean.
        continue-on-error: true
        run: pip-audit --requirement requirements.txt --requirement requirements-dev.txt

  frontend:
    name: Frontend
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Type check
        run: npx tsc -b

      - name: Build
        run: npm run build

      - name: Security scan (npm audit)
        # Non-blocking for now -- surfaces vulns without breaking the build.
        # Flip to `continue-on-error: false` once the baseline is clean.
        continue-on-error: true
        run: npm audit --audit-level=high

  e2e:
    name: E2E Tests
    runs-on: ubuntu-latest
    needs: [backend, frontend]
    if: >
      github.event_name == 'pull_request' &&
      (github.base_ref == 'main' || github.base_ref == 'production')

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: backend/requirements.txt

      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install backend dependencies
        run: pip install -r backend/requirements.txt

      - name: Install frontend dependencies
        run: cd frontend && npm ci

      - name: Install Playwright browsers
        run: cd frontend && npx playwright install --with-deps chromium

      - name: Run E2E tests
        run: cd frontend && npx playwright test
        env:
          E2E_MOCK: "true"
          ORS_API_KEY: "e2e-mock-key"
          CI: "true"

      - name: Upload Playwright report
        uses: actions/upload-artifact@v4
        if: ${{ !cancelled() }}
        with:
          name: playwright-report
          path: frontend/playwright-report/
          retention-days: 7
